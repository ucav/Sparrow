[package]
name = "sparrow-core"
version = "0.10.3"
edition = "2024"
description = "Core event and runtime contracts for Sparrow"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }
